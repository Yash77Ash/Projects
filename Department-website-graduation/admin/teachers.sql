CREATE TABLE `teachersinfo` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `teacher_photo` varchar(255) NOT NULL,
  `teacher_name` varchar(255) NOT NULL,
  `teacher_subject` varchar(255) NOT NULL,
  `teacher_file` varchar(255) NOT NULL,
  PRIMARY KEY (`id`)
) ;
